# Lifecycle Overview

## Lifecycle Sequence

When formal unit governance is needed, the standard lifecycle is:

```
unit_new / unit_fork → unit_check → unit_impl → unit_verify → unit_promote
```

- `unit_check` is a quality gate that validates whether candidate truth is clear enough. It can be
  re-run as a spec re-validation when candidate truth changes during the implementation phase
  (`Next Command=unit_verify`, `Notes=pending_impl`) — see `unit_check.md` precondition exception.
- `unit_impl` is a non-command phase between check and verify — the agent implements the candidate truth
- `unit_verify` verifies whether the implementation satisfies the candidate truth
- `unit_promote` promotes the verified candidate truth to stable truth

## Entry Method

The primary execution guide is the per-state directive generated by:
```text
specflowctl next --unit <name>
```
The directive tells you **TASK** (what to do), **READS** (files to read), **WRITES** (files you may modify), **BLOCKED** (hard boundaries), and **COMPLETION** (how to close when done). It is the deterministic instruction for the current governance step.

If the directive is insufficient, run:
```text
specflowctl next --unit <name> --explain
```
for full lifecycle context including the matching lifecycle Context Card content. Using the directive before reading lifecycle files guarantees you act only within authorized boundaries.

If `specflowctl` is unavailable, read `framework/operations/entry_routing.md` and the matching lifecycle Context Card at `framework/lifecycle/unit_*.md` to determine the correct action.

Both exact command matching (`command:{unit}`) and natural language are supported.

## Entry Commands

| Command | Purpose |
|---------|---------|
| `unit_init:{unit}` | Existing capability → first stable truth |
| `unit_new:{unit}` | Brand new → first candidate truth |
| `unit_fork:{unit}` | Stable truth → candidate change round |
| `unit_check:{unit}` | Candidate truth quality check (re-runnable during implementation for spec re-validation) |
| `unit_verify:{unit}` | Verify implementation vs candidate truth |
| `unit_promote:{unit}` | Candidate truth → stable truth |
| `unit_stable_verify:{unit}` | Check implementation vs stable truth |

`unit_impl:{unit}` is a trigger command — it provides implementation context to the agent without changing lifecycle state. It is valid when `Next Command=unit_verify`. After implementation, run `unit_verify:{unit}`. There is no `command close` for `unit_impl`. If spec issues are found during implementation, fix the spec and re-run `unit_check:{unit}` for re-validation (see `unit_check.md`).

## Command Execution Rules

- `command close` is the only operation that can advance lifecycle state
- Advancing evidence (`unit_check pass`, `unit_verify ready_to_promote`, `unit_stable_verify advancing`) requires an independent review receipt
- `unit_promote` consumes already-verified evidence and does not need a new independent review
- Non-advancing results (blocked, fix_required, evidence_incomplete) do not block subsequent correct evidence from advancing

## Dependency Management

- A candidate unit may depend on current stable-layer unit versions or current candidate truth (when the Context Card allows it)
- Stable-layer promotion must not silently change the stable versions consumed by other units
- When a stable version changes, `framework/governance/impact_sync.md` must be run to check the impact

## Rule Consumption

- Global rules automatically apply to all current-layer units
- Bound rules apply only when the unit explicitly lists them in `rule_refs`
- Rule changes are managed through `framework/governance/rule_system.md`

## Lifecycle State

`docs/specs/_status.md` records each unit's current state (layer, Next Command).
Only `command close` may modify this file. When `specflowctl` is unavailable, see the
Tooling-Unavailable Fallback for command close below.

### Tooling-Unavailable Fallback for command close

When `specflowctl command close` is unavailable (tooling not installed, broken, or
inaccessible), the executor may perform a manual close following the deterministic rules
below. This is the **only** exception to the rule that `command close` is the sole
mechanism for advancing lifecycle state.

**Manual close is scoped to the current lifecycle command only.** It must not be used
to skip lifecycle phases, jump ahead in the lifecycle sequence, or perform close
operations that involve automatic file mutations that manual file editing cannot
reliably reproduce.

#### Pre-conditions (mandatory — all must pass)

1. All required writes from the current Context Card's outcome are complete and correct.
2. All process evidence files are written with the correct schema (see
   `framework/process_snapshot_contract.md` for the file format).
3. For advancing outcomes (`pass`, `ready_to_promote`, `aligned`, `controlled_repair_required`,
   `controlled_change_required`, `truth_text_change_required`): the independent evaluation
   receipt is present in the process evidence, satisfying the gate rule requirements from
   `framework/core/independent_evaluation.md` Section Gate Rules.
4. The `_status.md` file is readable and the target unit's current `Next Command` matches
   the command being closed.

If any pre-condition fails: STOP, report what is missing, and do not perform the manual close.

#### Deterministic state transition

Use the active Context Card's How to End outcome table to determine the new `Next Command`
and `Notes` for the target unit:

- Each outcome row in the table specifies the writeback semantics in its "Next Step"
  column. The `Next Command` after close is the value shown in the table's writeback
  description (e.g., `command close sets Next Command=unit_verify`).
- If the outcome table shows `keeps` instead of `sets`, the `Next Command` does not change.
- If the outcome table shows an explicit `Notes` value, include it; otherwise clear `Notes`.

Manual state mapping (command → outcome → `_status.md` update):

| Command | Outcome | Stable | Candidate | Active Layer | Next Command | Notes |
|---------|---------|--------|-----------|-------------|-------------|-------|
| unit_init | stable_created | yes | no | stable | unit_fork | (cleared) |
| unit_new | candidate_created | no | yes | candidate | unit_check | (cleared) |
| unit_fork | candidate_created | yes | yes | candidate | unit_check | (cleared) |
| unit_check | pass | — | — | — | unit_verify | caller-supplied |
| unit_check | checkpoint | — | — | — | unit_check | (cleared) |
| unit_check | fix_required | — | — | — | unit_check | (cleared) |
| unit_check | blocked | — | — | — | unit_check | (cleared) |
| unit_verify | ready_to_promote | — | — | — | unit_promote | (cleared) |
| unit_verify | truth_fallback | — | — | — | unit_check | (cleared) |
| unit_verify | spec_issue | — | — | — | unit_check | (cleared) |
| unit_verify | evidence_incomplete | — | — | — | unit_verify | (cleared) |
| unit_verify | human_verify | — | — | — | unit_verify | (cleared) |
| unit_verify | impl_issue | — | — | — | unit_verify | (cleared) |
| unit_verify | blocked | — | — | — | unit_verify | (cleared) |
| unit_stable_verify | aligned | — | — | — | unit_fork | (cleared) |
| unit_stable_verify | controlled_repair_required | — | — | — | unit_fork | (cleared) |
| unit_stable_verify | controlled_change_required | — | — | — | unit_fork | (cleared) |
| unit_stable_verify | small_repair_required | — | — | — | unit_stable_verify | (cleared) |
| unit_stable_verify | truth_rejudge_required | — | — | — | unit_stable_verify | (cleared) |
| unit_stable_verify | truth_text_change_required | — | — | — | unit_fork | (cleared) |
| unit_stable_verify | evidence_incomplete | — | — | — | unit_stable_verify | (cleared) |
| unit_stable_verify | blocked | — | — | — | unit_stable_verify | (cleared) |
| unit_promote | promoted | — | — | — | unit_fork | (cleared) |
| unit_promote | promotion_recovered | — | — | — | unit_check | (cleared) |
| unit_promote | verify_invalid_truth | — | — | — | unit_check | (cleared) |
| unit_promote | verify_invalid_binding | — | — | — | unit_check | (cleared) |
| unit_promote | verify_invalid_baseline | — | — | — | unit_check | (cleared) |
| unit_promote | verify_invalid_rule | — | — | — | unit_check | (cleared) |
| unit_promote | verify_invalid_gate | — | — | — | unit_check | (cleared) |
| unit_promote | verify_invalid_evidence | — | — | — | unit_verify | (cleared) |

> **Important:** The lifecycle state table above captures the standard transitions.
> Always consult the active Context Card's How to End outcome table as the authoritative
> source. If the Context Card's outcome table describes additional side effects
> (file creation, deletion, or evidence cleanup), perform those manually before the
> state update.

#### Manual `_status.md` update procedure

1. Read the current `docs/specs/_status.md`.
2. Locate the target unit's row:
   - If a row **exists** for the target unit, use it as the update target.
   - If **no** row exists (applies to `unit_init` and `unit_new`), add a new row
     with the following initial values from the mapping table:
     * `unit_init` → `stable_created`: `Stable=yes`, `Candidate=no`,
       `Active Layer=stable`, `Next Command=unit_fork`, `Notes=(cleared)`
     * `unit_new` → `candidate_created`: `Stable=no`, `Candidate=yes`,
       `Active Layer=candidate`, `Next Command=unit_check`, `Notes=(cleared)`
3. Set the `Next Command` field to the value from the deterministic mapping above.
4. Set or clear `Notes` per the mapping.
5. For `unit_fork`, set `Active Layer` to `candidate` per the mapping table.
    For `unit_promote` with outcome `promoted`: set `Active Layer` to `stable`,
    `Stable` to `yes`, and `Candidate` to `no` — this mirrors the `--apply`
    side effect that the manual close must reproduce.
    For all other commands, do **not** change `Active Layer`.
6. Write the updated `_status.md`.

#### Post-close cleanup

After updating `_status.md`, perform the cleanup described in the Context Card's outcome
table. Common cleanup patterns:

- `fix_required` and `blocked` outcomes: delete `_check_work/unit/{unit}.md` and
  `_check_result/unit/{unit}.md` (if they exist).
- Non-advancing verify outcomes: delete `_verify_result/unit/{unit}.md` (if it exists).

#### Recording the fallback

Write a short annotation to the command's process evidence file (if one exists) noting
that the close was performed via the tooling-unavailable fallback. Add the following to
the file's frontmatter or as a trailing section:

```yaml
command_close_fallback: manual
command_close_fallback_recorded_at: <UTC ISO 8601 timestamp>
```

This annotation documents that manual intervention occurred and is consumed by subsequent
executors only as advisory context — it is not a lifecycle gate validation input.

## Lifecycle Context Card

Each lifecycle command has a Context Card at `framework/lifecycle/unit_*.md`.
These files define the command procedure for each state. They are consumed by `specflowctl next`
as the source of lifecycle reference material and are also available for direct reading when
`specflowctl` is unavailable.

Sections:

1. **Input** — files that must be read for this step
2. **What This Step Does** — the goal and execution content of the current command
3. **Not Allowed** — hard boundaries
4. **How to End** — pass/fail/blocked results and the next step

The directive from `specflowctl next` is the primary execution guide. Read the lifecycle Context
Card when you need the full procedure detail (check items, outcome tables, pre-checks).

**Exception — `unit_impl`:** As a trigger command that does not change lifecycle state, produce process evidence, or execute a `command close`, `unit_impl` may adjust this layout: its Input section may contain a Condition paragraph (pre-execution prerequisite), and its How to End section uses prose instead of an outcome table (there is only one terminal outcome with no command close). The four standard section labels remain the same for discoverability.

> **Terminology note:** SpecFlow has one directive type and one card type. (1) **Directive** — produced by `specflowctl next`, the primary execution authority that returns TASK, READS, WRITES, BLOCKED, and COMPLETION for the current governance step. (2) **Lifecycle Context Card** (also called **Per-Command Card**) — the lifecycle file at `framework/lifecycle/unit_*.md`, consumed by `specflowctl next` as the source of lifecycle reference material and also available for direct reading. When any file uses "Context Card" without qualification, it refers to the lifecycle file.

`framework/...` paths in a Command Context Card are relative to the framework root:
- Installed project: `framework/...` → `specflow/framework/...`
- Source repository: `framework/...` → `framework/...`

## Lifecycle Permission Rules

`command close` is the only way to advance lifecycle state.

Advancing evidence is valid only when:
1. The current Context Card allows that evidence to be written
2. The current process file passes the corresponding `snapshot validate-process` check
3. The process file contains a valid independent review receipt (when required by the Context Card)
4. `command close` accepts the result and evidence

Valid input evidence is consumptive — only files that pass deterministic validation may be consumed.

Non-advancing results (blocked, fix_required, checkpoint) do not permanently disqualify subsequent work. After repair, the current evidence may still advance as long as it passes validation and carries an independent review receipt.
