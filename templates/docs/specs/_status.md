# Spec Status

> This file is the formal object status registry. It carries only current state facts and does not carry governance rules. The responsibilities of `_status.md`, the meaning of its fields, when it must be read, and its fallback rules are defined by `specflow/framework/core/status.md`.

## ⚠️ Empty State Guard

This table is currently empty. No units are formally governed yet.

When the table is empty:
1. Default adoption mode: **reader-only**. You MUST NOT write implementation code.
2. If the user requests code work: report that no units are registered, suggest the smallest legal first step (e.g. `unit_new:{name}` or choosing an adoption mode), and ask which path to take.
3. Do not assume code permission from an empty status.

## Formal Objects

| Object Type | Object | Stable | Candidate | Active Layer | Next Command | Notes |
|---|---|---|---|---|---|---|
